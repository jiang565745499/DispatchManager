@page "/DispatchClass"
@using DispatchManager.Components.Shared.TaskClass
@inject DispatchManager.DataAccess.FreeSql.Service.DispatchClassService MainDb
@inject DispatchManager.DataAccess.FreeSql.Service.SqliteLogService LogDb

<PageTitle>任务系统</PageTitle>

@{
    var classList = MainDb?.GetAllDispatchClass() ?? [];
    var classCount = classList.Count;
    var colorCount = classList.Count(c => !string.IsNullOrEmpty(c.FColor.ToString()));
}

<div class="page-container">
    <div class="content-wrapper">
        <!-- 统计卡片 -->
        <div class="row g-4 mb-4">
            <div class="col-md-3">
                <div class="modern-card p-4 text-center fade-in stat-card-item">
                    <div class="mb-3"><i class="fas fa-layer-group fa-3x text-primary"></i></div>
                    <h4 class="fw-bold mb-2 stat-value">@classCount</h4>
                    <small class="text-muted fw-500">总分类数</small>
                </div>
            </div>
            <div class="col-md-3">
                <div class="modern-card p-4 text-center fade-in stat-card-item">
                    <div class="mb-3"><i class="fas fa-palette fa-3x text-success"></i></div>
                    <h4 class="fw-bold mb-2 stat-value">@colorCount</h4>
                    <small class="text-muted fw-500">已配置颜色</small>
                </div>
            </div>
            <div class="col-md-3">
                <div class="modern-card p-4 text-center fade-in stat-card-item">
                    <div class="mb-3"><i class="fas fa-chart-line fa-3x text-warning"></i></div>
                    <h4 class="fw-bold mb-2 stat-value">100%</h4>
                    <small class="text-muted fw-500">系统状态</small>
                </div>
            </div>
            <div class="col-md-3">
                <div class="modern-card p-4 text-center fade-in stat-card-item">
                    <div class="mb-3"><i class="fas fa-cogs fa-3x text-info"></i></div>
                    <h4 class="fw-bold mb-2 stat-value">100%</h4>
                    <small class="text-muted fw-500">可用率</small>
                </div>
            </div>
        </div>

        <!-- 表格卡片 -->
        <div class="dtc-card fade-in">
            <div class="dtc-card-header">
                <div class="dtc-card-header-left">
                    <h5 class="dtc-card-title">任务分类列表</h5>
                    <span class="dtc-card-badge">@classCount 条记录</span>
                </div>
            </div>
            <div class="dtc-card-body">
                <Table TItem="DispatchClass" ExtendButtonColumnWidth="270" IsFixedHeader="false" SortString="CreateTime desc"
                       IsPagination="true" PageItemsSource="@PageItemsSource"
                       ShowExtendButtons="true"
                       IsAutoRefresh="true"
                       ShowExportButton="true"
                       ShowToolbar="true" ShowDefaultButtons="true" ShowSearch="true" IsMultipleSelect="true"
                       SelectedRows="SelectedRows"
                       ShowEditButtonCallback="OnShowButtonCallback"
                       ShowDeleteButtonCallback="OnShowButtonCallback"
                       OnQueryAsync="OnQueryAsync"
                       OnDeleteAsync="OnDeleteAsync"
                       OnSaveAsync="OnSaveAsync">
                    <TableColumns>
                        <TableColumn @bind-Field="@context.ClassName" Sortable="true" Filterable="false" Searchable="true" TextWrap="true">
                            <Template Context="v">
                                <div class="d-flex align-items-center">
                                    <i class="fas fa-tag text-primary me-2"></i>
                                    <span>@v.Value</span>
                                </div>
                            </Template>
                        </TableColumn>
                        <TableColumn @bind-Field="@context.FColor">
                            <Template Context="v">
                                <div class="d-flex align-items-center">
                                    <div class="me-2" style="background-color: @v.Value; width: 16px; height: 16px; border-radius: 50%; border: 2px solid #e2e8f0;"></div>
                                    <Tag Color="v.Value">颜色标识</Tag>
                                </div>
                            </Template>
                        </TableColumn>
                    </TableColumns>
                    <EditTemplate>
                        <TaskClassEditor @bind-Value="context" />
                    </EditTemplate>
                </Table>
            </div>
        </div>
    </div>
</div>
